import React from 'react';
import QuoteHistory from '../../components/customer/dashboard/quoteHistory/QuoteHistory';

const CustomerQuoteHistory: React.FC = () => {
  return <QuoteHistory />;
};

export default CustomerQuoteHistory;
